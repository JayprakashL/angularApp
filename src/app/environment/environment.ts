// Example for Value providers (Refering Angular 13 for now)
// Const variable 'env' is exported from environment.ts and this apiEndpoint needs to be accessed.

export const env = {
    production: false,
    apiEndpoint: 'http://localhost:8080/apiPath/version3'
};